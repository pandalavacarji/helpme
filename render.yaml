services:
  - type: web
    name: access-controller-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2
    envVars:
      - key: PORT
        value: 9000
    healthCheckPath: /health
